# ############# Raylib powered minisynth project ###############

# To work, brew must be installed.

# Install libraries needed :

brew install pkgconfig
brew install raylib

# Run bash script to compile :

bash compile.sh

# Run the program with a songfile as argument :

./minisynth <file>

# Good luck !
